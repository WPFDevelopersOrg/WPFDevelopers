using System;
using System.Diagnostics;
using System.Text;
using System.Windows.Controls;

namespace WPFDevelopers.Samples.ExampleViews
{
    /// <summary>
    /// ScrollViewerAnimationExample.xaml 的交互逻辑
    /// </summary>
    public partial class ScrollViewerAnimationExample : UserControl
    {
        public ScrollViewerAnimationExample()
        {
            InitializeComponent();
            //string[] logs = new string[] { "Application", "System" };
            //foreach (string log in logs)
            //{
            //    EventLog myLog = new EventLog();
            //    myLog.Log = log;
            //    EventLogEntryCollection eventLogEntryCollection = myLog.Entries;
            //    foreach (EventLogEntry entry in eventLogEntryCollection)
            //    {
            //        string info = string.Empty;
            //        if (entry.EntryType == EventLogEntryType.Error || entry.EntryType == EventLogEntryType.Warning)
            //        {
            //            info += "类型：" + entry.EntryType.ToString() + ";";
            //            info += "日期" + entry.TimeGenerated.ToLongDateString() + ";";
            //            info += "时间" + entry.TimeGenerated.ToLongTimeString() + ";";
            //            info += "来源" + entry.Source.ToString() + ";";
            //            Console.WriteLine(info);
            //        }
            //    }
            //}
            
        }
    }
}
