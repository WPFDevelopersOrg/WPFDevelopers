<UserControl
    x:Class="WPFDevelopers.Samples.ExampleViews.Basics.ListBoxExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:WPFDevelopers.Samples.Controls"
    xmlns:wd="https://github.com/WPFDevelopersOrg/WPFDevelopers"
    Background="{DynamicResource WD.BackgroundBrush}">
    <controls:CodeViewer Header="ListBox">
        <StackPanel Margin="20,10">
            <TextBlock Margin="0,20,0,0" FontSize="20" Text="ListBox" />
            <UniformGrid Height="300" Margin="0,10" Columns="2">
                <ListBox
                    wd:ElementHelper.CornerRadius="3"
                    DisplayMemberPath="Name"
                    FontSize="20"
                    ItemsSource="{Binding UserCollection, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <ListBox Margin="10,0" ItemsSource="{Binding UserCollection, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <UniformGrid Columns="3">
                                <TextBlock Text="{Binding Date}" />
                                <TextBlock Text="{Binding Name}" />
                                <TextBlock Text="{Binding Address}" />
                            </UniformGrid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </UniformGrid>
        </StackPanel>
        <controls:CodeViewer.SourceCodes>
            <controls:SourceCodeModel CodeSource="/WPFDevelopers.SamplesCode;component/ExampleViews/Basics/ListBoxExample.xaml" CodeType="Xaml" />
        </controls:CodeViewer.SourceCodes>
    </controls:CodeViewer>
</UserControl>
